# Candidate handoff · 2026-08-15

## Exact candidate

- Worktree: `/Users/jiawei/hivecosm-worktrees/hivecrew-operations-workflow-v2`
- Branch: `work/hivecrew-operations-workflow-v2`
- Implementation commit: `9249b3e00`
- Runtime scope: frontend `127.0.0.1:13512`, backend `18592`, candidate DB
  `multica_hivecrew_operations_workflow_v2_512`.

## What changed

1. The Workflow page now reads the persisted event ledger per scoped instance,
   renders durable receipt rows, and derives a read-only node-status projection
   from the immutable graph/version plus persisted instance state. Selected
   instance identity is URL-backed; a completed terminal index is displayed as
   `流程完成 / 4/4`, not a fictitious fifth stage.
2. The local candidate login path canonicalizes only a mismatched loopback
   hostname before starting the already guarded local operator session. It
   preserves strict-cookie security and does not accept a remote client.
3. Sidebar test fixtures were extended for the already-present Workflow and
   other navigation routes so the workflow change is tested in the real
   navigation contract.

## Browser and database evidence

- Host Chrome authenticated from `127.0.0.1:13512/login` through the canonical
  loopback route and reached the existing candidate workspace.
- A candidate-only L3 `验收：公众号内容生产` was created and edited, then bound
  to the existing candidate L4 Project. The browser displayed the completed
  four-node canary and seven persisted receipts, including two rejected
  advances.
- A separate unmapped temporary L3 was created and deleted through the second
  confirmation. Candidate DB readback: one retained operating program, one
  retained L3/L4 mapping, and zero temporary-program rows.
- The Project outcomes tab honestly rendered no Outcome Center result; no
  Outcome, Artifact, NAS, removable-disk, or cloud record was created.

## Verification passed

- `go test ./internal/workflow ./internal/handler ./internal/companyops`
- `pnpm --dir packages/views exec vitest run workflow/workflow-workbench.test.tsx workflow/workflow-operations-page.test.tsx layout/app-sidebar.test.tsx`
- `pnpm --dir apps/web exec vitest run features/auth/canonical-loopback-login.test.ts 'app/(auth)/login/page.test.tsx'`
- `pnpm --dir apps/web exec vitest run 'app/[workspaceSlug]/(dashboard)/workflow/workflow-runtime-readback.test.ts'`
- `pnpm --dir packages/core exec vitest run api/workflow.test.ts api/schemas.test.ts`
- `pnpm --filter @multica/web typecheck`
- `pnpm --filter @multica/web build`

## Deliberate boundary and known review gate

- No main merge, production deployment, production DB migration, external
  publication, trading, NAS/cloud write, or destructive real storage action.
- The complete `packages/views` suite has four untriaged failures outside this
  workflow change: `layout/sidebar-resize.test.tsx`,
  `agents/components/agent-activity-hover-content.test.tsx`, and two cases in
  `layout/tab-presentation.test.tsx`. They are not waived. Investigate them
  independently before merge/deploy; do not use this candidate evidence to
  claim a whole-repository green build.

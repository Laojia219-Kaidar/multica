/goal HIVECREW-OWNER-OPERATING-WORKBENCH-V1 v2 — Prime 0813 主力驱动的四车道正式融合

## v2 当前执行指令（2026-08-14）

继续同一个 Goal，不创建第二套项目或状态真源。以正式 HiveCrew Project `3b0330e7-a2da-4f41-94ab-61c911af2820`、本目录 `CHECKLIST.yaml` 和干净融合工作树 `/Users/jiawei/hivecosm-worktrees/hivecrew-four-lane-formal-convergence-codex` 为唯一当前执行面，把已经形成候选的四条 Prime 车道正式消费为一套可构建、可验收、可回滚的 HiveCrew 产品候选：

- W1：公司 OS 信息架构、组织/基地、数字员工工厂、数据知识工厂、QM Workroom 候选。
- W2：项目持续经营、停止/继续、生命周期闭环、成果归属与回执候选。
- W3：遗留项目收敛、组合集成、运行时与浏览器验收证据。
- W4：工作流、员工记忆、绿色终端实时工作墙候选。

主力数字员工为 `Shard｜后端与数据库工程师`（Agent `86b86d76-09f4-42e3-bb7a-8113c81775e0`），主 Runtime 为 `Prime Agent · DeepSeek V4 Pro 0813`，模型路由必须为 `deepseek/deepseek-v4-pro`。优先基地为工程基地 MBP M5X；远端 Prime 适配失败时自动降级到中枢基地 Mac mini 的同一 Runtime Profile，不让整条车道等待。Shard 是唯一正式融合写入者，不垄断调研、合同、fixtures、测试、视觉校验和独立审查。

### 当前阶段

执行 `P2F — 四车道正式融合`：

1. `F0 运行时 canary`：真实 HiveCrew Issue→Task→Prime Run→Task-linked Comment 全链路通过。
2. `F1 四车道 intake`：冻结每条车道的 revision、dirty boundary、候选文件、测试与 receiver，不重新实现已完成工作。
3. `F2 并行消费`：组织/基地、项目生命周期、工作流/记忆/工作墙、视觉合同和测试矩阵并行；只冻结真实冲突文件。
4. `F3 单一 Join`：Shard 在本工作树消费候选，禁止其他 Writer 覆盖共享文件、migration 号段和发布配置。
5. `F4 独立验收`：Gauss 做合同/回归，Atelier 做返修/集成，Keep 做构建与运行时回读；reviewer 不得等于实现者。
6. `F5 当前系统应用`：精确 revision 构建后，以可回滚事务更新 HiveCrew `:3000/:8080`，浏览器验证议题、项目、数字员工、组织部门、工作墙、成果中心与运行回执；1421 仍留给原 `P7` 正式发布事务。

### 速度优先的冲突规则

- W1–W4 原工作树视为只读候选源；不得要求它们为 W3 或本 Join 停产。
- 冲突只锁精确文件、migration 号段、端口或部署 critical section；无冲突的合同、UI、测试、fixtures、审查继续并行。
- 任一任务 held 超过 10 分钟，必须写明 blocker、receiver、wake condition，并转入同一 Goal 的非冲突 fallback；禁止无声等待。
- Task 数、token 消耗、Issue 状态和“审核中”不等于有效产出；评价指标是 accepted candidate、Task-linked receipt 和运行时应用延迟。
- 审核只阻断与 required VictoryCondition 直接相关的错误；可逆改进进入 Repair backlog，不让全部员工停工。

### 视觉标准

- 不另造第二套设计系统；复用当前 HiveCrew tokens、布局、导航、表格/卡片密度、暗色/亮色和中英文 i18n。
- 新栏目必须与现有侧边栏、页面标题、空态、加载态、错误态、按钮层级和响应式行为一致。
- 工作墙保持绿色终端卡片语义，但字体、边框、间距、悬停、抽屉和详情交互必须来自当前组件体系。
- 验收必须同时包含真实数据、空态、失败态、窄屏/宽屏和浏览器截图；禁止只用 mock 或静态截图宣称完成。

### v2 新增胜利条件

`VC-07`：W1–W4 的 required 候选被一个干净、可重建的精确 revision 消费，真实 HiveCrew UI 可见相关功能，Shard 的 Prime 0813 Run 及各员工 Task/Run/Comment lineage 可回读，独立审查和浏览器旅程通过，当前 HiveCrew 运行时与该 revision 一致且存在回滚点。

以下 v1 使命、永久边界、P0–P7 与 VC-01–VC-06 继续有效；v2 是当前执行 frontier，不删除历史目标。

## 使命与人类结果

以 William 已经实际使用的 HiveCrew 为唯一日常交互与执行底座，保留 Inbox、Chat、My Issues、Issues、Projects、Automation、Agents、Squads、Usage、Runtimes、Skills、Settings，并通过 HiveCosm 反腐适配层连接公司权威事实。首先交付一条 William 可以亲自完成的真实闭环：需求进入收件箱或对话，形成并确认 WorkOrder，精确绑定 Employee 与 Agent，产生真实 Run、Execution Receipt 和 Temporary Artifact，支持反馈、返工、验收与 Promotion，最终在项目和员工档案留下 Formal Artifact 引用。随后在同一产品内完成 Outcome Center、正式组织与员工管理、跨主机执行资源、QM 真人协作和 1421 候选整合。

成功时，William 日常通过一个工作台管理数字员工、未来真人员工、项目、执行和成果；系统不再只是快照，也不再因某个模型名或单个 Agent 身份忙碌而阻塞其他项目。

## 当前真实起点

- 当前主机：`jiaweis-Mac-mini.local`。
- 仓库 / worktree：`/Users/jiawei/hivecosm-worktrees/hivecrew-b2-design`，分支 `work/wo-hivecrew-b2-foundation-design`。
- 激活基线：`43d7c95dfbf50e3b53a328b2c35a6bd36a5ddf1d`；B1 候选基线为 `85d927ebc3a3ecd2f685f1f932865f9906c2c446`。
- 已验证基础：HiveCrew 独立源码保管和 B1 产品重识别候选；现有本地工作台实际提供 Inbox、Chat、My Issues、Issues、Projects、Automation、Agents、Squads、Usage、Runtimes、Skills、Settings。
- 已完成规划复核：`docs/reviews/RP-HIVECREW-OPERATING-WORKBENCH-20260811-01.md`，可见 ChatGPT Pro GPT-5.6 Sol / Pro effort 评审结论 `APPROVE WITH CONDITIONS`。
- 已冻结设计边界：`HIVECREW.md`、`docs/architecture/WRITE-AUTHORITY-MATRIX.md`、`docs/architecture/HIVECREW-B2-COMPANY-OBJECT-WORKFORCE-MODEL.md`。
- 当前差距：设计存在但尚未有一条经过真实 UI、真实 writer、真实 Run、真实回执和正式成果引用共同证明的闭环；Outcome Center、正式 Employee/Organization、分布式容量和 QM 联合工作尚未实现。
- 当前正式生产状态：未将本 Goal 候选应用到 DGX、1421 或生产数据库。

## 最终成果、范围与永久不变量

### 必须交付

1. 一个真实可重复的 Owner-to-Outcome 纵向闭环。
2. 一个最小但可操作的 Outcome Center：发现、预览、版本、血缘、反馈、返工、接受、Promotion 和运营资产引用。
3. Department、Position、Appointment、Employee 的正式管理投影与精确 AgentBinding；现有 Agents 页面继续管理执行身份与配置。
4. 一个 Employee 可使用多个 Execution Profile、Runtime Instance、Model Route 并拥有多个并行 Run 的可验证能力。
5. 真人与数字员工共享 Employee/Project/WorkOrder/Outcome 语义，QM 只作为 Workroom/协作上下文。
6. 经过行为测试、构建和内置浏览器验收的可回滚候选；最终 1421 切换只通过本 Goal 中独立的发布 Work Order 执行。

### 永久不变量

- HiveCrew 只拥有交互态与执行态：Conversation、Session、Assignment、Task Run、Execution Receipt、Temporary Artifact。
- HiveCosm 继续拥有公司权威态：Department、Position、Appointment、Employee、Company Project、Governance、Knowledge、Formal Artifact。
- Agent 不自动等于 Employee；Squad/Team 不等于 Department；API 不等于 Runtime；Model、Endpoint、Harness 与 Runtime 分离。
- Temporary Artifact 不等于 Formal Artifact；正式成果只能通过显式 Promotion 命令建立引用。
- 禁止第二员工、部门、项目、WorkOrder、成果、治理或知识真源；选择和写入只使用稳定原生 ID 与显式 IdentityBinding。
- 不使用数组索引、显示名、模型名、第一会话或列表顺序做业务身份。
- 不以 UI 本地状态变化冒充真实写入成功；写入必须返回可见 receipt 并触发精确 refetch。
- 不引入 Multica 上游跟踪；不写入 secret value；不使用 `git add .` 或 `git add -A`。

### 明确不改变

- Phase 1 前不修改 DGX、1421、正式数据库、公司注册表或正式服务。
- 不为了完成首条闭环而先重写完整知识图谱、完整组织 Graph、完整 QM 或 100–200 员工集群。
- 不以全面导航重构、卡片快照或抽象 Gate 替代真实闭环。

## Goal-wide standing autonomy

- 自动允许：所有范围内读取；使用 Team/subagent 从 ready frontier 派发不重叠任务；通过内置浏览器进行脱敏 GPTPro 复核；隔离 worktree 内的范围化编辑；行为测试、typecheck、build、候选预览、浏览器验收、常规修复；CHECKLIST、JSONL 工作日志、证据、Review Package、Mermaid 投影、Resume Package、Obsidian 日记和归档写入；满足本 Goal 声明前置条件的可回滚候选整合及独立发布 Work Order。不得为这些动作重复要求 William 点击或确认。
- Owner escalation：目标改变、实质扩围、权威真源冲突、员工身份最终登记冲突、法律/财务/秘密或凭据决定、无法回滚的破坏性动作、外部商业发布、以及会改变胜利条件的证据矛盾。
- 平台硬互锁、凭据缺失或单个高风险节点受阻时，如实记录并继续其他安全 ready nodes，不停止其他 ready 工作，不伪造完成。
- Out of scope：未列入 Goal Graph 的公司业务运营、客户真实生产交付、真实金融交易、法律签署和无关 HiveCosm 页面重构。

## 唯一真源与统筹

- 执行状态唯一真源：`docs/goals/HIVECREW-OWNER-OPERATING-WORKBENCH-V1/CHECKLIST.yaml`。
- 代码状态真源：当前 Git worktree 和精确 revision。
- 证据真源：CHECKLIST 注册的证据文件、测试输出、浏览器旅程和运行 receipt。
- Mermaid、HTML、Obsidian、聊天总结和仪表盘只是投影或追加记录，不反写执行状态。
- Active Orchestrator：当前 Codex 主代理，Employee identity 未绑定；它是唯一 active controller 和 Join/Integrator，只统筹主线、ready frontier、互斥、整合、证据与完成判定，不以自身记忆取代 CHECKLIST。
- Controller 变更必须写入 `active_controller`、`claimed_at`、`previous_controller`、`reason` 和 Resume Package 后才能接管。

## Goal Graph

- 节点类型：Outcome、VictoryCondition、Phase、WorkOrder、Worker、Artifact、Evidence、Review、Journal、Projection、Decision。
- 边类型：decomposes_to、depends_on、parallel_with、mutex_with、assigned_to、produces、verified_by、satisfies、reviewed_by、recorded_in、projects_to、unlocks、corrects。
- 当前 ready frontier：`WO-P1-01-FRONTEND-TRUTH`、`WO-P1-02-BACKEND-WRITERS`、`WO-P1-03-OUTCOME-CHAIN` 并行只读审计；完成后进入 `WO-P1-04-CONTRACT-JOIN`。
- Critical path：`P0 -> P1 -> P2 -> P3 -> P7 -> OUTCOME-01`。
- `P4 Organization`、`P5 Distributed Workforce`、`P6 QM Workrooms` 在 P2 后按写入边界并行推进，但 P7 只在所需胜利条件有证据后汇合。
- 所有 hard dependency 为 DAG；评审纠偏以新 Work Order 的 `corrects` 边回指，不把反馈环写入硬依赖 DAG。

## Team 多线程执行

- 当至少两个无依赖、无 write_scope 冲突、无 mutex 冲突节点 ready 且有并发槽位时，从 ready frontier 并行派发。
- 每个 Work Order 必须列明输入、read_scope、write_scope、mutex_keys、Worker 角色、产物、验收、verifier、evidence target 和 fallback。
- Worker 完成只形成候选；Join/Integrator 统一检查 diff、行为测试、运行 receipt、来源边界和浏览器旅程后才能更新 CHECKLIST。
- Team 或某个 Worker 失败时由 Orchestrator 重新分配或本地执行该有界节点，其他 ready nodes 继续。
- 不用 Agent 数量冒充有效并行度；优先关键路径并为整合和验收保留容量。

## GPTPro 三段复核

1. 规划冻结前：architecture_plan，已使用 `hivecosm-gptpro-review-board` 完成；证据见 Review Package。
2. 每个 major Phase 结束前：drift/evidence review。
3. Goal 完成前：final victory review。

每次使用 Codex 内置浏览器，展开模型选择器并记录 Pro capability 的 label、URL、timestamp 和 screenshot 或等价 visual evidence。无法证明时记录 `PRO_CAPABILITY_UNVERIFIED`，不得把该响应称为 GPTPro。GPTPro 只是顾问，不授予生产、发布、合并或完成权限。每轮一次主审、最多一次聚焦质询。意见分类固定为 `phase_critical_correction`、`evidence_gap`、`optional_improvement`、`out_of_scope`、`owner_decision`；只有影响 required victory、evidence integrity、永久边界或 false-completion 的问题阻塞。

## Phase 0 — 底座、权威和规划冻结

- 目标：确认现有 HiveCrew 是真实底座，冻结 Write Authority、Agent/Employee、Project/WorkOrder 和 Temporary/Formal Artifact 边界。
- Work Orders：`WO-P0-01-ARCH-REVIEW`。
- 成果：Review Package、权威矩阵、B2 对象模型和本 Goal 激活决定。
- 验证与证据：Review Package validator、GPTPro `APPROVE WITH CONDITIONS`、Owner 本轮激活、已验证 Obsidian marker。
- GPTPro review：`REVIEW-P0` 已关闭。
- Obsidian milestone：`hivecosm-work-journal` 已 append、回读并 verify；细粒度状态不写入日记，保留在 JSONL/CHECKLIST。
- Exit / Victory：规划边界和第一 ready frontier 可执行；不声称任何产品实现完成。

## Phase 1 — 真实链路与最小合同

- 目标：精确找到现有 UI、canonical writers、运行回执和成果载体，冻结一页实施 canvas 与 frontend-to-runtime manifest。
- Work Orders：P1 三条只读审计并行，`WO-P1-04-CONTRACT-JOIN` 汇合。
- 成果：`PHASE-1-OPERATING-LOOP-CONTRACT.md`，包含 pilot 对象选择规则、稳定 ID、读写 owner、writer、receipt、refetch、失败态、候选 URL 和 rollback。
- 验证与证据：精确源码/行号、接口 schema、现有测试、无第二 writer/真源审计。
- GPTPro review：`REVIEW-P1`。
- Obsidian milestone：`JOURNAL-P1` 使用 hivecosm-work-journal append/read-back/verify；细粒度协调留在 JSONL。
- Exit / Victory：首条闭环的每个写动作都有唯一 writer 和 receipt；缺口明确且不会以假按钮填充。

## Phase 2 — 第一条真实 Owner-to-Outcome 闭环

- 目标：在现有 HiveCrew 中实现并浏览器验收一条真实需求、派工、执行、临时成果、反馈、返工、验收和正式成果引用闭环。
- Work Orders：行为测试先行；最小 HiveCosm anti-corruption adapter；WorkOrder/AgentBinding 上下文；真实 Run/Receipt；Temporary Artifact；Review/Promotion 引用；共享前端操作面；Join/Integrator。
- 成果：可发现路由、真实 context list、稳定 URL、canonical writer、可见 receipt、错误保留、候选服务和回滚说明。
- 验证与证据：组件行为测试、Go/API 测试、typecheck/build、真实但有界 pilot Run、内置浏览器同路径验收、receipt 与 Formal Artifact 引用回读。
- GPTPro review：`REVIEW-P2`。
- Obsidian milestone：`JOURNAL-P2` append/read-back/verify。
- Exit / Victory：VC-01 与 VC-02 有当前证据；禁止以 API 响应或截图替代正式 UI reachability。

## Phase 3 — Outcome Center

- 目标：把 P2 的成果闭环扩展为一级 Outcome Center，而不是文件夹。
- Work Orders：Outcome 列表/筛选；详情/预览；版本和血缘；Review Queue；Published/Operational Assets；项目/员工/Run 反向引用。
- 成果：可按 Project、Employee、Type、Status 发现和操作成果。
- 验证与证据：真实 P2 成果贯穿列表、详情、审阅、版本、发布状态；行为测试和浏览器验收。
- GPTPro review：`REVIEW-P3`；Obsidian：`JOURNAL-P3`。
- Exit / Victory：VC-03 有当前证据，Temporary/Formal 权威未混淆。

## Phase 4 — 正式组织与员工

- 目标：提供 Department、Position、Appointment、Employee 的注册投影、组织图、名册、档案和精确 AgentBinding；保留 Squad/Team 的执行语义。
- Work Orders：反腐读取适配器；冲突/未登记报告；组织结构；员工名册与 dossier；候选 Agent-to-Employee 映射和 Owner/治理确认命令。
- 成果：公司身份与执行资源明确分层；一个 Employee 可拥有多个 Execution Profile。
- 验证与证据：重复 Employee/AgentBinding fail closed；未登记不伪造；员工点击打开精确 dossier/对话；行为测试与浏览器验收。
- GPTPro review：`REVIEW-P4`；Obsidian：`JOURNAL-P4`。
- Exit / Victory：VC-04 的身份部分有当前证据。

## Phase 5 — 跨主机分布式执行资源

- 目标：证明一个 Employee 可在多个 Host/Runtime/Profile 上拥有多个独立 Run，并建立 Host、RuntimeInstance、Capability、CapacityLease、Queue、Retry、Backpressure 和 Receipt 的最小控制能力。
- Work Orders：只读资源目录；调度合同；两个运行实例有界 canary；容量与恢复 UI；失败/重试演练。
- 成果：不再把模型名或单个 Agent 当全局单线程锁。
- 验证与证据：多实例并行 Run、独立 session/worktree/sandbox/receipt、容量释放、失败恢复和无跨任务串线。
- GPTPro review：`REVIEW-P5`；Obsidian：`JOURNAL-P5`。
- Exit / Victory：VC-04 全部满足；完整 100–200 规模压测可作为后续扩展而不阻塞商业闭环。

## Phase 6 — QM 真人与数字员工 Workroom

- 目标：真人与数字员工共享 Employee、Project、WorkOrder 和 Outcome 语义，通过 QM Workroom 共同讨论、审阅、交付和回读。
- Work Orders：Human Binding；QM Workroom adapter；混合参与者任务/讨论；正式状态回写；冲突与离线恢复。
- 成果：QM 是协作上下文，不成为第二注册表、项目或成果真源。
- 验证与证据：一名真人与一名数字员工在同一 WorkOrder/Outcome 上协作并留下精确引用和 receipt。
- GPTPro review：`REVIEW-P6`；Obsidian：`JOURNAL-P6`。
- Exit / Victory：VC-05 有当前证据。

## Phase 7 — 导航收敛与 1421 发布事务

- 目标：在不复制工作台的前提下，使 HiveCrew 成为 1421 的交互/执行内核。
- Work Orders：基于已工作的页面重组 L1/L2/L3；保留底座功能；增加 Outcome、Organization、Execution Resources、Workrooms；候选构建；正式发布前只读预检；独立可回滚发布 Work Order。
- 成果：中文/英文、深色/浅色和布局符合 1421 标准；正式端口只在候选浏览器验收、备份、精确 revision、rollback 和服务边界满足后切换。
- 验证与证据：候选 URL、正式 URL、服务 revision、UI 旅程、重启/回滚演练、生产回读。
- GPTPro review：`REVIEW-P7` 与 Goal final victory review；Obsidian：`JOURNAL-P7`。
- Exit / Victory：VC-06 和所有 required VictoryCondition 有当前证据。

## Phase 执行循环

校准主线 → 重算 ready frontier → 并行派工 → join/integrate → verify → evidence → GPTPro review → 关闭必要纠偏 → Obsidian append/read-back/verify → refresh projections → Phase completed → 下一 frontier。

## 失败、阻塞与恢复

- 状态使用 planned、ready、executing、integrating、verifying、reviewing、completed、held、failed、rollback_pending、superseded、abandoned。
- 单节点 held/failed 不停止其他安全 ready 工作；fallback 为重新分配或由 Orchestrator 本地完成。
- Resume Package 必须维护 goal/version、current controller、current phase/wave、ready/running/blocked、evidence index、latest review/journal、dirty boundaries 和 next action。
- 任何运行、发布或写入失败必须保留草稿、精确目标、错误和 rollback 路径；不得因 UI 关闭或进程存活声称成功。

## 胜利条件

1. `VC-01`：William 可从现有工作台发现入口，以稳定 ID 选中真实需求和员工，形成并确认 WorkOrder，触发精确 Agent/Runtime Run，并看到真实 receipt。
2. `VC-02`：同一旅程产生 Temporary Artifact，支持反馈和返工，验收后通过显式 Promotion 建立 Formal Artifact 引用，并在 Project 与 Employee 上可回读。
3. `VC-03`：Outcome Center 能发现、预览、筛选、审阅、版本化和追踪至少一项真实成果，不退化为文件列表。
4. `VC-04`：Employee 与 Agent/Runtime/Model 分层，一个 Employee 在至少两个独立执行实例上拥有多个 Run，身份和 receipt 不串线。
5. `VC-05`：真人与数字员工通过 QM Workroom 在同一 WorkOrder/Outcome 上协作且不产生第二真源。
6. `VC-06`：经过候选验收的 HiveCrew 能以独立可回滚发布事务成为 1421 交互/执行内核，正式 UI、服务和 revision 可回读。

## 完成规则

只有所有 required VictoryCondition 都有当前证据、所有 required Work Order 和 Phase exit 已关闭或被证明不影响胜利条件、最终 GPTPro review 已记录、所有 Phase 日记和投影已验证、正式发布状态与候选状态未混淆、没有剩余必需工作时，才标记 Goal complete。百分比、单个绿色测试、打开页面、完成底座、Worker 自报或预算耗尽均不能替代完成。

## 立即开始

执行当前 P1 三条并行只读审计；Join 后写出一页最小合同和首条 behavioral test 计划；随后直接进入 P2 的测试先行实现，不再开启新的总体规划循环。

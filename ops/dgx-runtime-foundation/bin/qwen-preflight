#!/usr/bin/env bash
set -euo pipefail
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd); command -v jq >/dev/null || exit 127; qwen=$(command -v qwen 2>/dev/null || true); if [[ -n "$qwen" ]]; then ver=$("$qwen" --version 2>&1 || true); printf "qwen_cli=present version=%s\\n" "$ver"; else printf "qwen_cli=missing\\n"; fi; printf "landlock_preflight=required\\nno_model_call=true\\nsecret_values_recorded=false\\n"
